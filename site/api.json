{
  "mint-nft": {
    "repo": "mint-nft",
    "install": "npm ci --force --progress=false",
    "run": "anchor build"
  },
  "stake-spl": {
    "repo": "stake-spl",
    "install": "npm ci --force --progress=false",
    "run": "anchor build"
  }
}
